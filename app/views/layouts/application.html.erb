<!DOCTYPE html>
<html lang="ja">
<head>
  <%# ↓ 更新時にキャッシュを保持して反映が遅くならないように。デバッグ後は消すこと %>
  <%# <meta name="turbo-cache-control" content="no-cache"> %>
  <%# ↑ 更新時にキャッシュを保持して反映が遅くならないように。デバッグ後は消すこと %>
  <title><%= full_title(t("#{controller_path}.#{action_name}.title", default: t("titles.default"))) %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
      /* Minimal override: use Noto Sans JP globally */
      body { font-family: 'Noto Sans JP', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Hiragino Kaku Gothic ProN', 'Hiragino Sans', 'Yu Gothic UI', 'YuGothic', 'Meiryo', 'Helvetica Neue', Arial, sans-serif; }
    </style>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>

<body class="min-h-screen flex flex-col text-neutral-900 antialiased leading-7" style="background-color:#E8DFCA;">
  <% unless controller_name == "sessions" || (controller_name == "users" && action_name == "new") %>
    <%= render "shared/header" %>
  <% end %>


  <main class="flex-grow container mx-auto px-4 md:px-6 lg:px-8 my-6">
    <%= render "shared/flash" %>
    <%= yield %>
  </main>

  <footer class="text-center py-2 text-sm text-gray-700 mt-4" style="background-color:#B7CBB2;">
    &copy; 2025 Murasaki_67a App
  </footer>
</body>


</html>
